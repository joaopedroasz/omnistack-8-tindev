import React from 'react';
import './App.css';

import Routes from './routes'; // Importando o componente.

// Componente no ReactJS:
// Não é nada mais do que uma função que retorna um template HTML.
function App() {
  return (
      <Routes /> // Tratando o componente Login como uma tag HTML normal.
  );
}

export default App;
